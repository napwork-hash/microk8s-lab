apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-service
spec: 
  rules:
    - host: lab.napskytech.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-deployment
                port:
                  number: 8080