apiVersion: v1
kind: Service
metadata:
  name: portfolio-stg-svc
  namespace: portfolio-stg
spec:
  type: NodePort
  selector:
    app: portfolio-stg
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30090