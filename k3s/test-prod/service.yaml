apiVersion: v1
kind: Service
metadata:
  name: portfolio-prod-svc
  namespace: portfolio-prod
spec:
  type: NodePort
  selector:
    app: portfolio-prod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30100